export enum Langs {
  es = 'es',
  en = 'en',
}

export enum RoutesTypes {
  questions = '/questions',
  home = '/home',
  podim = '/podium',
}

export const topTime = 120

export enum LocalStorageTypes {
  users = 'users',
}
